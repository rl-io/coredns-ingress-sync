# Default values for coredns-ingress-sync
replicaCount: 1

image:
  repository: ghcr.io/rl-io/coredns-ingress-sync
  pullPolicy: IfNotPresent
  # tag defaults to Chart.AppVersion

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext:
  fsGroup: 65534
  runAsNonRoot: true
  runAsUser: 65534
  runAsGroup: 65534

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop:
    - ALL

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 10m
    memory: 64Mi

# Metrics configuration
metrics:
  enabled: true
  port: 8080
  path: /metrics
  # Service configuration
  service:
    annotations: {}
    labels: {}
  # ServiceMonitor configuration (for Prometheus Operator)
  serviceMonitor:
    enabled: false
    interval: 30s
    scrapeTimeout: 10s
    labels: {}
    annotations: {}

# Health check configuration  
healthCheck:
  enabled: true
  port: 8081
  path: /healthz

nodeSelector: {}

tolerations: []

affinity: {}

# CoreDNS configuration
coreDNS:
  # Automatically configure CoreDNS for dynamic DNS resolution
  # IMPORTANT: Set to true to enable automatic CoreDNS configuration
  # When false, manual CoreDNS configuration is required
  autoConfigure: false
  # Namespace where CoreDNS is deployed
  namespace: kube-system
  # Name of the existing CoreDNS ConfigMap to modify
  configMapName: coredns

# Controller configuration
controller:
  # Multi-class configuration (preferred).
  # An ordered list of ingress class -> target CNAME mappings. List order is the
  # default priority tiebreak: the first entry has the lowest implicit priority,
  # so when two ingresses share a hostname the later-listed class wins unless a
  # per-ingress priority annotation overrides it.
  # When set, this takes precedence over the legacy ingressClass/targetCNAME
  # pair below. Leave empty ([]) to use the single-class settings.
  ingressClassMappings: []
  #  - ingressClass: nginx
  #    targetCNAME: "ingress-nginx-controller.ingress-nginx.svc.cluster.local."
  #  - ingressClass: traefik
  #    targetCNAME: "traefik.traefik.svc.cluster.local."

  # Legacy single-class configuration. Used only when ingressClassMappings is
  # empty, and kept for backward compatibility.
  # Ingress class to watch
  ingressClass: "nginx"
  # Target CNAME for DNS resolution
  targetCNAME: "ingress-nginx-controller.ingress-nginx.svc.cluster.local."
  # Namespace filtering - empty means watch all namespaces
  # Set to comma-separated list to watch specific namespaces: "default,production,staging"
  watchNamespaces: ""
  # Exclusion filters
  # Namespaces to exclude (comma-separated). Applied after watchNamespaces.
  excludeNamespaces: ""
  # Ingresses to exclude (comma-separated). Supports name or namespace/name.
  excludeIngresses: ""
  # Annotation key to enable syncing (set to false to disable on a given ingress)
  annotationEnabledKey: "coredns-ingress-sync-enabled"
  # Annotation key for per-ingress priority (integer, higher wins). When two
  # ingresses share a hostname, the one with the higher value wins the rewrite.
  annotationPriorityKey: "coredns-ingress-sync-priority"
  # Log level: debug, info, warn, error
  logLevel: "info"
  
  # Dynamic ConfigMap configuration (created by this controller)
  dynamicConfigMap:
    name: "coredns-ingress-sync-rewrite-rules"
    key: "dynamic.server"
  
  # Volume name for mounting dynamic configuration
  volumeName: "coredns-ingress-sync-volume"
  
  # Mount path for dynamic configuration (will be auto-generated if not set)
  # Default: /etc/coredns/custom/{deployment-name}
  mountPath: ""
  
  # Environment variables (for advanced configuration)
  env: {}

# RBAC configuration
rbac:
  # Specifies whether RBAC resources should be created
  create: true

# Job configuration
jobs:
  # How long to keep failed preflight jobs for debugging (in seconds)
  # Set to 0 to delete immediately, or increase for longer debugging time
  failedJobTTL: 300  # 5 minutes

# Leader election configuration
leaderElection:
  # Enable leader election for high availability
  enabled: true
